import React from 'react';
import {Box} from "@material-ui/core";

export const Chart = ({ flex }) => {
    return (
        <Box flex={flex}>
            <span>Chart here</span>
        </Box>
    );
};
